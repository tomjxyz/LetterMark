.h LetterMark

This is a note written in lettermark, a markup language designed for taking
notes quickly.

The parsing program (lmcomp) quickly takes inputs like this, and turns them into
PDFs, with many formatting options.

Here is a link to the repo:
.a https://github.com/tomjxyz/LetterMark
